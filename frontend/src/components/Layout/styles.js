const styles = theme => ({
  root: {
    display: 'flex',
    flexFlow: 'column',
    minHeight: '100vh'
  },
  content: {
    padding: theme.spacing(0, 8),
    flex: 1
  }
});

export default styles;
