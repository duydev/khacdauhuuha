const styles = theme => ({
  root: {},
  logo: {
    padding: theme.spacing(1),
    margin: theme.spacing(0, 16)
  },
  logoImage: {
    height: '60px',
    cursor: 'pointer'
  }
});

export default styles;
